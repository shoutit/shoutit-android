<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <android.support.v7.widget.Toolbar
        android:id="@+id/select_profile_toolbar"
        style="@style/BaseToolbarNoElevation"
        android:layout_width="match_parent"
        android:layout_height="?actionBarSize"/>

    <android.support.design.widget.TabLayout
        android:id="@+id/select_profile_tablayout"
        android:layout_below="@+id/select_profile_toolbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/colorPrimary"
        app:tabTextColor="@color/green_tab_text_color"
        app:tabSelectedTextColor="@android:color/white"
        app:tabMode="fixed"
        app:tabMaxWidth="0dp"
        />

    <android.support.v4.view.ViewPager
        android:id="@+id/select_profile_view_pager"
        android:layout_below="@+id/select_profile_tablayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"/>

    <View
        android:layout_width="match_parent"
        android:layout_below="@+id/select_profile_tablayout"
        android:layout_height="3dp"
        android:background="@drawable/toolbar_drop_shadow"/>

    <include layout="@layout/base_progress"/>

</RelativeLayout>